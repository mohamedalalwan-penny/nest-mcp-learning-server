# Generate a value with: openssl rand -hex 32
MCP_AUTH_TOKEN=replace-with-a-random-token-at-least-16-characters

# Optional comma-separated allow-list. Requests without Origin are accepted.
MCP_ALLOWED_ORIGINS=http://localhost:6274

PORT=3000
